<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- External storage (Download folder) -->
    <external-path 
        name="external_files" 
        path="Download/" />
    
    <!-- App cache (fallback) -->
    <cache-path 
        name="cache" 
        path="." />
    
    <!-- External files -->
    <external-files-path 
        name="external_app_files" 
        path="." />
</paths>